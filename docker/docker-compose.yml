services:
  kindle_bot:
    container_name: kindle_bot_container
    image: kindle_bot_image:1.0.0
    build:
      context: ..
      dockerfile: docker/Dockerfile_local
    ports:
      - "80:5000"
    env_file: ../secrets.env
