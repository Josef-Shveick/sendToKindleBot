services:
  kindle_bot_aws:
    container_name: kindle_bot_aws_container
    image: kindle_bot_aws_image:1.3.3
    build:
      context: ..
      dockerfile: docker/Dockerfile_aws

  kindle_bot_local:
    container_name: kindle_bot_local_container
    image: kindle_bot_local_image:0.0.1
    build:
      context: ..
      dockerfile: docker/Dockerfile_local
    env_file: ../secrets.env
    environment:
      - POOLING=true
    volumes:
      - C:\Users\ruslan.sereda\Desktop\sendToKindle\sendToKindleBot\attachments:/app/attachments
